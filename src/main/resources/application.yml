spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dockercicd
    username: root
    password: a327548123!
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8080